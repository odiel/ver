import { Grid, CircularProgress } from '@mui/material';

export default () => ( <Grid container justifyContent="center" sx={{marginTop: '124px'}}>
        <CircularProgress size={'76px'} thickness={2} />
    </Grid>
);